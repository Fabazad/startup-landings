"use client";

import { User } from "src/app/wewish/types/User";


// ----------------------------------------------------------------------

export const Credentials = ({ user }: { user: User }) => {
    console.log({ user })
    return null
}
